// src/config/apiConfig.js

const API_CONFIG = {
  USER_SERVICE: "http://localhost:8081",
  POST_SERVICE: "http://localhost:8015",

  // later (when API Gateway comes)
  // GATEWAY: "http://localhost:8080"
};

export default API_CONFIG;
